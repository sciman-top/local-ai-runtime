# skills

- 仓路径：`D:\CODE\external\local-ai-dev-orchestrator-references\skills`
- 当前本机锚点：`main @ 49f948faa9`

## 为什么保留

这个仓已经被上游标记为 deprecated，因此它现在只保留为 `archive/on-demand` 参考，而不是当前主入口。

继续保留它，主要是为了这些回看场景：

- 回看历史 `skill` 目录结构是怎样演进过来的
- 对照旧 `SKILL.md` 组织方式与新 `plugin` 打包方式的差异
- 兼容旧 `skills-drafts/` 语境时，不必重新去找旧入口

## 什么时候先看它

- 要核对旧 skill 结构或迁移旧 skill 草稿时
- 要确认某段历史文档里的 `skills` 指代的是哪个旧入口时

## 什么时候不要扩大到它

- 当前要找最新官方示例时，应先看 `plugins`
- 当前 Hermes runtime 或 MCP 协议排查，不要先跳到这里

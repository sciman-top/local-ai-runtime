# LAR-P0A-010 Q0/gate/limit contract closure evidence

## Objective and truth boundary

- Baseline: `local-ai-runtime-0.2-v3.25` (`baseline_candidate`).
- Plan/task: `local_ai_runtime_work_items.v4 / LAR-P0A-010`.
- Current runtime truth remains `runtime/host-orchestrator` plus `.ai/state/control-plane.db`.
- Target artifact: `P0A-Q0 / QualificationGateCatalog.v1`.
- This slice authors and verifies offline contracts. It does not run live Q0, create `FullQ0Record`, activate a runtime, inspect live auth/provider/DPAPI/sandbox state, or create `runtime/local-ai-runtime`.

## Write set and identities

| Member | Bytes | SHA-256 |
|---|---:|---|
| `normative/QualificationGateCatalog.v1.json` | 16513 | `bad2899fa6af96af2800d05d6c78e0e17ebf9ac08ecb5d89b68518630a31070b` |
| `catalogs/CodexFeaturePolicy.v1.json` | 4274 | `6b61e82dfb4051b6da52cfd104ed3ed9b070e076d914bf45f3c06f345c1865aa` |
| `catalogs/ResourceLimitPolicy.v1.json` | 9009 | `d28327965428f2be9789758cb47aba7fcc2f279de3d6ed8c48a19f57221ef590` |
| `fixtures/q0/manifest.json` | 10990 | `efed4e6cb06a1c1d1a11c44721c316274894cc915087908f186461898952f708` |

The bundle is verified by `scripts/q0_gates_limits_contract.py`, exposed through `scripts/verify-local-ai-runtime-baseline.py --component q0-gates-limits`, and covered by `test_baseline_q0_gates_limits.py`. Inventory, planning status, machine work items, operator docs and this repo-level evidence are the only other projections. Frozen v3.24 bytes, carried artifacts, `.ai/config`, `.ai/state` and legacy runtime behavior are outside the write set.

## Acceptance closure

- GateGraph fixes `supply_chain_identity -> build -> test -> contract_invariant -> hotspot`; six gate-run rows bind a typed executable manifest member, exact argv, environment digest, cwd final identity, timeout, report schema and failure scope. Managed sandbox/Git profiles require `--include-managed-config`; placeholder or ellipsis mutations fail closed.
- `Q0TriggerPolicy.v1` classifies binary/model/profile/feature/adapter/environment/schema/Windows/Git and unknown diffs as Full Q0, while allowing scoped requalification only for same-envelope narrowing with unchanged capability/effect protocol.
- The activation chain requires composition, Implementation Acceptance, Full Q0, selected identity, pointer CAS and immediate quick preflight before active identity. Q0 never selects or activates a runtime.
- `ProcessEnvironmentPolicy.v1` starts empty, has a 14-key public allowlist, ASCII catalog spelling, `OrdinalIgnoreCase` uniqueness, hidden/alias/NUL rejection, UTF-16LE ordering and exact double-NUL termination.
- `pre_resume_parent_environment_proof` binds parent input bytes, grammar, uniqueness, order, limits, terminator, digest and CreateProcess flags. `post_resume_q0_child_environment_observation` is a dedicated no-write child's first post-resume application action. Production launches persist a per-launch parent proof and never claim per-child pre-resume read-back.
- `ProcessHandlePolicy.v1` binds atomic JOB_LIST + HANDLE_LIST creation, `STARTF_USESTDHANDLES`, closed parent child-ends, no ambient sensitive handles and EOF proofs for normal exit, kill, crash recovery and response loss. Job termination alone never proves EOF.
- External evidence is fixed to `runtime_external_v1` with handle/alias isolation. DPAPI envelopes have separate quarantine/integrity purposes, while application generation, purpose and single-consumption invariants remain explicit rather than inferred from DPAPI.
- `ResourceLimitPolicy.v1` contains all 28 frozen main ceilings plus six explicit process-input/EOF ceilings. The fixture manifest provides 84 main-resource and 18 process-input/EOF `limit-1/limit/limit+1` observations plus nine environment semantic cases. `accounting_kill_audit` uses pre-spawn snapshots, continuous watcher, at-most-500-ms fallback, durable kill intent, drain and final no-follow audit.
- `EmergencyDiskReserveRecord.v1` fixes a controller-only fully allocated 1 GiB reserve and suspended release/rebuild lifecycle. `HardWriteQuotaCapability.v1` remains optional and Full-Q0-qualified; missing capability retains mandatory accounting mode. Disk pressure maps to `resource_exhausted_recovery_pending`, never `needs_environment`.
- Failure scope distinguishes attempt, template, repo and platform, with successful unauthorized egress and enforcement drift escalating to platform incompatibility.

## Verification evidence

| Command | Result |
|---|---|
| `python -m py_compile scripts/q0_gates_limits_contract.py scripts/verify-local-ai-runtime-baseline.py` | exit 0 |
| `python scripts/verify-local-ai-runtime-baseline.py --component q0-gates-limits` before identities were frozen | expected fail; semantic validation reached final identity gate and returned `q0_catalog_identity` |
| `uv run --project .\runtime\host-orchestrator python -m pytest runtime/host-orchestrator/tests/test_baseline_q0_gates_limits.py -q` | exit 0; `6 passed` |
| `uv run --project .\runtime\host-orchestrator python -m pytest` | exit 0; `317 passed` |
| `python scripts/verify-local-ai-runtime-baseline.py --component q0-gates-limits` | exit 0; `28` main limits, `6` process-input/EOF limits, `84 + 18` boundary observations, `9` environment cases, `12` internal negative mutations |
| `python scripts/verify-planning-status.py` | exit 0; package `10/15`, five missing artifacts, current `LAR-P0A-011` |
| `python scripts/select-next-work.py` | exit 0; unique `LAR-P0A-011 / close_baseline_normative_package_first`, no side effects |
| `python -m py_compile scripts/q0_gates_limits_contract.py scripts/verify-local-ai-runtime-baseline.py scripts/verify-planning-status.py scripts/select-next-work.py` | exit 0 |
| PowerShell `ConvertFrom-Json` over seven changed machine JSON surfaces | exit 0; `parsed=7` |
| frozen v3.24 candidate/inventory/plan byte+SHA-256 comparison | pass; all three exact identities unchanged |
| `git diff --check` | exit 0 |
| `pwsh -NoProfile -NonInteractive -File scripts/governance/preflight.ps1 -DisableAutoCommit` | exit 0 on evidence-bearing bytes; build/hotspot retained the approved planning N/A records, test=`317 passed`, contract/selector/scripts/diff green |

The first focused test run was an intentional red step: the baseline verifier rejected unknown component `q0-gates-limits`. After the component was implemented, the focused and full suites passed. Release-style preflight is run on the final evidence bytes before commit.

## Gate N/A records

| Gate | Status | reason | alternative_verification | evidence_link | expires_at | recovery_condition |
|---|---|---|---|---|---|---|
| build | `gate_na` | P0A authors contracts; `runtime/local-ai-runtime` does not exist | host-orchestrator pytest plus component verifier | this note | `LAR-P0D-001` | no-side-effect package scaffold provides the real build entry |
| hotspot | `gate_na` | no runtime hot path changed | focused/full planning tests, verifier, selector and `git diff --check` | this note | first executable slice after `LAR-P0D-001` | executable runtime slice activates real hotspot profile |

These N/A records preserve planning order `build -> test -> contract/invariant -> hotspot`; environment preparation and live Q0 are not substituted for a gate.

## Compatibility, risk and rollback

The v3.25 narrative and all v3.24/history/carried artifact bytes remain immutable. This bundle implements the already-frozen two-stage proof and narrows unsupported claims; it does not change product scope, `capacity=1`, B3 deferral, P0C/P0D join, external behavior or live data.

Rollback reverts only the four Q0 bundle members, component verifier/test integration, `P0A-Q0` inventory identity and current planning/docs/evidence projections. It restores `LAR-P0A-010=ready`, `LAR-P0A-011=pending`, package `9/15 present`, and the prior baseline-entry identity. It must not rewrite frozen narrative/history, carried artifacts, `.ai/config`, `.ai/state` or legacy runtime files.

## Verified closeout

The package remains incomplete and approval-ineligible. `P0A-Q0` is present contract evidence, not a Full Q0 result. The next eligible item after final closeout is `LAR-P0A-011`; approval, Truth Reset, implementation, Full Q0 and P2 remain inactive.

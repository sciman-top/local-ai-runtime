# 任务清单

## 已完成

- [x] 建立长期维护仓目录骨架
- [x] 固化 `Windows 主力机 AI Agent 分层架构 v1.6`
- [x] 固化实施计划
- [x] 固化任务清单
- [x] 固化社区参考源码放置策略
- [x] 固化参考项目清单
- [x] 固化当前交接摘要
- [x] 固化接手检查单
- [x] 固化工作交接提示词
- [x] 固化交接摘要模板
- [x] 固化 `.env.example`
- [x] 固化 `.gitignore`，避免真实 `.env` 误提交
- [x] 把当前 `AgentBridge` 的安全快照复制到仓内
- [x] 保留本机私有运行数据的本地落点，但不纳入 git
- [x] 初始化 git 并提交当前维护基线
- [x] 把上游参考源码拉到 `D:\CODE\external\hermes-agent-references`
- [x] 改进 `verify-hermes-boundary.ps1`，让 `service_uidgid_present` 直接观测到受监管 `hermes chat` 进程
- [x] 修复 `run-hermes-wrapper.sh` fail-closed，避免 Hermes 失败却被包装层记成成功
- [x] 修复默认 `--oneshot` bring-up 路径，恢复 `invoke-hermes-bringup-once.ps1` 默认主链
- [x] 修复 `start-hermes.ps1` 非交互 stdout 吞没问题，恢复 `chat -q` / `--oneshot` 终端回显
- [x] 修复交互式 `chat --cli` 假死，恢复 classic REPL 直接接管宿主 TTY
- [x] 修复 known-good snapshot 秒级文件名冲突，避免同秒并发时备份文件锁误伤

## 下一步

- [x] 固化本机 `Phase 1` 能力探针报告
- [x] 把探针结论回填到 `README / 实施计划 / 当前交接摘要`
- [x] 明确 `Phase 1` 当前只允许“纯本地任务自动执行 MVP”
- [ ] `Doc Sync`：落盘 `Hermes-v2-终态收口-20260628.md`
- [ ] `Doc Sync`：落盘 `Hermes-v2-路线图.md`
- [ ] `Doc Sync`：落盘 `Hermes-v2-实施规格.md`
- [ ] `Doc Sync`：落盘 `Hermes-v2-接口规格.md`
- [ ] `Doc Sync`：落盘 `Hermes-v2-runner-协议.md`
- [ ] `Doc Sync`：落盘 `Hermes-v2-测试规格.md`
- [ ] `Doc Sync`：同步 `README / 实施计划 / 当前交接摘要 / 任务清单 / 接手检查单 / 工作交接提示词`
- [ ] `P0-1`：`cap_drop:[ALL]` 已完成运行探针，但在当前 `official_root_bootstrap` 运行模型下会打坏真实 `docker run` 路径；保持阻塞，等待运行模型变化后再评估
- [ ] `P0-2`：验证 `read_only rootfs + tmpfs` 是否在真实 bring-up 下稳定
- [ ] `P0-3`：把 Hermes 网络白名单保留为探针 slice，不再把它写成 `Phase 1` 前提能力
- [ ] `Phase 1`：先只实现 `single worker / single repo / SDK first / exec fallback / pure local tasks`
- [x] 给每个参考仓补一份“本机为什么保留它”的短注释或观察记录
- [x] 补一个批量更新参考仓并输出差异摘要的脚本
- [ ] 如果以后真要在 `Ubuntu-24.04` 里直接跑 `docker`，再补 Docker Desktop 的 per-distro WSL integration
- [ ] 如确有消息入口需求，再单独评估是否把 OpenClaw 从“参考仓储备”升级为主链渠道层候选

## Wave 1 原子任务

- [ ] `W1-T01`：搭建 `host-orchestrator` 项目骨架
- [ ] `W1-T02`：封装 `Codex SDK` 最小 worker 入口
- [ ] `W1-T03`：实现 `codex exec fallback`
- [ ] `W1-T04`：实现 `control-plane.db` 初始化
- [ ] `W1-T05`：实现 `host_local` 单任务执行闭环
- [ ] `W1-T06`：实现 Wave 1 smoke 样本任务集
- [ ] `W1-T07`：实现 Wave 1 验收脚本

## 当前收口判断

- [x] 当前 repo-side 主队列已闭环：方案、计划、任务清单、交接材料、AgentBridge 快照、boundary 证据、参考仓观察记录、参考仓刷新脚本与刷新摘要均已落盘
- [ ] `Ubuntu-24.04` 内直连 `docker` 仍是条件项，不因当前维护仓收口而自动视作已做
- [ ] `OpenClaw` 是否升级为主链候选仍取决于未来真实消息入口需求

## 升级门禁

每次升级 Hermes / provider / 运行方式前，必须满足：

- [ ] 已备份当前 `/opt/data` named volume
- [ ] 已记录当前 snapshot 与 compose hash
- [ ] 只变更一个变量
- [ ] 若未来再次评估 `cap_drop:[ALL]`，必须先重做真实 `docker run` 探针，不能只看 Compose 静态配置
- [ ] 已确认新 release tag 与 digest
- [ ] 已确认密钥仍为 session-only 注入
- [ ] 已重新跑契约检查
- [ ] 已重新跑 bring-up gates
- [ ] 已重新跑 boundary 验证
- [ ] 已重新生成 known-good snapshot

## 明确不做

- [ ] 不把真实密钥提交进 git
- [ ] 不把 volume 私有运行态备份提交进 git
- [ ] 不把 `D:\CODE\external` 的上游参考源码混进本仓历史
- [ ] 不把 Hermes 文本当成对 Codex 的直接授权

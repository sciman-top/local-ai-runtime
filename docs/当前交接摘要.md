# 当前交接摘要

更新时间：`2026-06-28`

## 1. 当前目标

这个维护仓服务于同一条主链：

- `Codex = 主力执行层`
- `Hermes = 隔离学习层`
- `PAD = 固定 GUI 补件`
- `OpenClaw = 暂缓`

当前 v1 目标仍然固定为：

`Hermes 生成任务 -> Codex 执行 -> Hermes 吸收结果`

也就是人工触发的批量学习闭环，不是实时自进化，不是常驻后台学习，也不是 Hermes 直接控制 Windows 桌面。

当前 active / planned 分层已明确：

- `active truth = v1.6 bring-up 已验收，可维护基线成立`
- `planned target = Phase 0 先做 v1.6 安全治理收口，Phase 1 再做 V2 Codex-Native 自动化 MVP；v2 target truth 见 [Hermes-v2-终态收口-20260628.md](D:/CODE/hermes-agent/docs/Hermes-v2-终态收口-20260628.md)`
- 最新本机探针已证明：`workspace-write` 与 SDK 可用，但 `network_proxy` 未证实，所以 `Phase 1` 当前必须收紧为“纯本地任务自动执行”
- `Wave 1` 当前已完成 repo-side `W1-T01 ~ W1-T07` fake-first 薄切片，但还没达到 accepted：
  - 已有 `runtime/host-orchestrator/` Python 工程骨架
  - 已有 Python `Codex SDK` worker 映射与最小执行包装
  - 已有 `codex exec` fallback 包装
  - 已有 `control-plane.db` 初始化与基础表结构
  - 已有 `host_local` 单任务 fake-first 闭环
  - 已有三类 Wave 1 smoke 样本任务集
  - 已有 `pytest -> contract -> smoke` 的 repo-side 验收脚本
  - 仍未做 live `Codex SDK` 真机执行验收

## 2. 当前真值位置

### 维护仓

- `D:\CODE\hermes-agent`

用途：

- 保存方案定稿
- 保存实施计划
- 保存任务清单
- 保存交接提示词与检查单
- 保存 `AgentBridge` 的安全快照与索引

### live 运行面

- `C:\Users\sciman\Documents\AgentBridge`

用途：

- 当前 Windows 侧实际运行与验证入口
- 一次性 bring-up、contract/gates/boundary/snapshot 脚本仍以这里的 live 树为准

### 外部上游参考源码

- `D:\CODE\external\hermes-agent-references`

用途：

- 保存可更新的上游源码真值
- 不把 vendor 历史混进维护仓

## 3. 当前已完成

当前判断：

- `repo-side done`
- `live-side done`：本轮已在修完 `chat --cli` 交互 wrapper 后重新实跑 Hermes bring-up，默认 `--oneshot` 主链继续通过

- 方案、实施计划、任务清单已落到维护仓
- `AgentBridge` 已验收快照已复制到维护仓
- 根目录真实 `.env` 与 volume 私有备份已从 git 提交范围隔离
- 外部参考源码已拉到本机
- `verify-hermes-boundary.ps1` 已改为直接观测受监管 `hermes chat` 进程，并已在 live 与快照两边留下最新 boundary 证据
- 7 个外部参考仓的保留理由观察记录已补齐
- 参考仓批量刷新脚本已落到 `scripts/refresh-reference-repos.ps1`
- 最新参考仓刷新摘要已落到 `references/updates/reference-refresh-latest.md`
- 参考仓现已按 `core / secondary / conditional` 分层，默认刷新只覆盖 core
- `ai.input.im` live 配置已切到命名 custom provider + `key_env` + `/v1` + `default_headers.User-Agent`
- `run-hermes-wrapper.sh` 已修成 fail-closed，不再把 Hermes 实际失败记成成功
- 已查明并修复默认 `--oneshot` bring-up 失败：
  - 根因不是用户 key 错，也不是 Docker 不稳
  - 根因是 Hermes upstream `oneshot` 把 `HERMES_INFERENCE_MODEL=gpt-5.4` 当成显式选模，再把 custom OpenAI-compatible 路由误判成不可路由分组别名 `openai`，最终报 `Unknown provider 'openai'`
  - 当前 bridge 侧修复是在 `--oneshot/-z` 且未显式传 `--model/--provider` 时，屏蔽 `HERMES_INFERENCE_MODEL/HERMES_INFERENCE_PROVIDER`，强制回退到 `config.yaml` 已持久化的 provider
- 已查明并修复“无反应/卡住”的直接原因：
  - 不是 Hermes 没输出
  - 是 `start-hermes.ps1` 原来把所有非交互 stdout 主动吞掉了
  - 现已改为转发 `chat -q` / `--oneshot` 的标准输出
- 已查明并修复 `chat --cli` 看起来“卡住”的根因：
  - 不是 Hermes 没启动
  - 是 `run-hermes-wrapper.sh` 之前把交互式 `hermes` 也包进命令替换，导致 classic REPL 的 stdout 不再是 TTY，终端被占住但提示符不可见
  - 现已在检测到交互式 `chat/auth/model/setup` 且 stdin/stdout 都挂着 TTY 时直接 `exec hermes "$@"`
- 默认 bring-up 已在修完 `chat --cli` 后重新实跑通过；随后 `P0-2` replacement runtime model 也已完成 live-side acceptance，最新 known-good 为：
  - `C:\Users\sciman\Documents\AgentBridge\docs\known-good-20260628-225738-431.json`
- 维护仓当前已有可追溯 git 基线；早期关键提交包括：
  - `ad5db41`：维护仓基线
  - `f4d5ddd`：参考项目清单与外部仓落点同步
  - 后续更新以当前 `git log` 为准
- `Wave 1` 当前新增 repo-side 资产：
  - `runtime/host-orchestrator/pyproject.toml`
  - `runtime/host-orchestrator/src/host_orchestrator/`
  - `runtime/host-orchestrator/tests/`
  - `runtime/host-orchestrator/fixtures/wave1-smokes/`
  - `runtime/host-orchestrator/scripts/run-wave1-smokes.ps1`
  - `runtime/host-orchestrator/scripts/test-wave1-acceptance.ps1`
  - 当前已通过：
    - `uv run pytest`
    - `snapshots/agentbridge-20260628/scripts/test-agentbridge-contract.ps1`
    - `runtime/host-orchestrator/scripts/test-wave1-acceptance.ps1`

## 4. 当前最重要证据

先看这几个文件，不要一上来就翻长聊天：

1. [Windows主力机-AI-Agent-分层架构-v1.6.md](D:/CODE/hermes-agent/docs/Windows主力机-AI-Agent-分层架构-v1.6.md)
2. [实施计划.md](D:/CODE/hermes-agent/docs/实施计划.md)
3. [参考项目清单.md](D:/CODE/hermes-agent/docs/参考项目清单.md)
4. [snapshots/agentbridge-20260628/docs/implementation-status.md](D:/CODE/hermes-agent/snapshots/agentbridge-20260628/docs/implementation-status.md)
5. [snapshots/agentbridge-20260628/docs/known-good-20260628-225738-431.json](D:/CODE/hermes-agent/snapshots/agentbridge-20260628/docs/known-good-20260628-225738-431.json)
6. [snapshots/agentbridge-20260628/docs/verify-hermes-boundary-20260628-225841-414.json](D:/CODE/hermes-agent/snapshots/agentbridge-20260628/docs/verify-hermes-boundary-20260628-225841-414.json)
7. [references/updates/reference-refresh-latest.md](D:/CODE/hermes-agent/references/updates/reference-refresh-latest.md)
8. [snapshots/agentbridge-20260628/docs/中文操作说明.md](D:/CODE/hermes-agent/snapshots/agentbridge-20260628/docs/中文操作说明.md)
9. [phase1-capability-probe-report-20260628-143744.md](D:/CODE/hermes-agent/docs/phase1-capability-probe-report-20260628-143744.md)
10. [AgentBridge-迁移与复用-runbook.md](D:/CODE/hermes-agent/docs/AgentBridge-%E8%BF%81%E7%A7%BB%E4%B8%8E%E5%A4%8D%E7%94%A8-runbook.md)

## 5. 当前真实边界

### 已确认

- Windows 侧 v1.6 bring-up 已实跑通过
- 当前默认 `invoke-hermes-bringup-once.ps1` 主链再次实跑通过
- 当前维护仓已把核心文档、脚本快照、日志元数据、known-good 元数据收进 git
- 外部参考源码已独立存放于 `D:\CODE\external\hermes-agent-references`
- `verify-hermes-boundary.ps1` 当前已直接命中 `10001:10001` 的 `hermes chat` 进程
- 外部参考仓当前已快进到最新可达 `main`，并有差异摘要留痕
- `AstrBot / NapCat` 不属于本参考架，QQ 渠道层参考应复用 `D:\CODE\external\qq-codex-bot-references`
- 非交互最小实证已确认：
  - `start-hermes.ps1 --oneshot 'Reply with exactly OK.'` 返回 `OK`
  - `start-hermes.ps1 chat -Q -q 'Reply with exactly OK.'` 返回 `OK`
- `chat --cli` 假死根因已定位并在 live wrapper 修复

### 仍要诚实保留

- `Ubuntu-24.04` 内直接使用 Docker Desktop integration 仍不是当前主路径
- `OpenClaw` 只是储备，不进 v1 主链
- Hermes upstream 的 `oneshot` provider 自动识别 bug 只是被 bridge 层绕过，尚未上游修复
- `chat --cli` 的最后一跳可见提示，本轮是在代码与非交互回归层面确认修复；宿主真实终端的视觉复核仍建议在 Windows Terminal / PowerShell 再跑一次
- `P0-1 cap_drop:[ALL]` 已不只是旧 `official_root_bootstrap` 历史结论；本轮已在当前 accepted replacement runtime model 上完成 repo-side 与 live-side 单变量复核，仍稳定打坏真实 `docker run` 路径，因此当前应固定为 `still blocked / not accepted`
- `P0-2 read_only rootfs + tmpfs` 已完成 live-side acceptance：
  - `/run` 必须至少使用 `tmpfs /run:exec`
  - 旧 accepted 运行模型 `official_root_bootstrap + 10001:10001 remap` 在 `read_only rootfs` 下仍稳定阻塞，这条历史结论没有被推翻
  - comparative `10000:10000` 线与 probe-only bootstrap 只用于缩窄 blocker，不应被误写成 accepted truth
  - 纯 `derived_non_root` 直启线也仍会失败在 `/run preinit`
  - 当前已正式采纳的新 accepted 运行模型是：
    - `runtime_image = agentbridge/hermes-nonroot:p0-2-20260628`
    - `bootstrap_model = derived_non_root`
    - `runtime_user = 10001:10001`
    - `container_start_user = 0:0`
  - 这条 replacement runtime model 已在 live `AgentBridge` 上完整通过 `bring-up -> boundary -> snapshot`
  - acceptance 过程中还顺手补齐了两个 live-side drift：
    - `manage-hermes-provider-session.ps1` 现已兼容当前机器仍在使用的 legacy indexed `.env` 变量名（如 `TEXT_MODEL_BASE_URL_1 / TEXT_MODEL_API_KEY_1`）
    - 当从外层再起一个新的 `pwsh -File` 时，`TmpfsMounts` 数组会被 PowerShell 调用边界拆坏；正式 acceptance 已确认应直接在当前 PowerShell 进程里调用
  - 因此当前 `P0-2` 的正确状态已经变成：`accepted replacement model live-side green`
  - 同时，`P0-1` 已在新 accepted 线复核后继续维持 blocked，`P0-3` 也已明确收口为 `platform_na / 非 Phase 1 前提`
  - 因而当前 `Phase 0` 的正确口径不是“全绿完成”，而是“运行面收口完成，剩余边界已被诚实固定”
  - 最新证据见 [phase0-readonly-rootfs-probe-20260628.md](D:/CODE/hermes-agent/docs/phase0-readonly-rootfs-probe-20260628.md)

## 6. 当前禁止事项

- 不要把真实 `.env` 提交进 git
- 不要把 `private-local/agentbridge-volume-backups/` 提交进 git
- 不要把外部参考仓混进 `D:\CODE\hermes-agent`
- 不要把 Hermes 任务文本当成对 Codex 的直接授权
- 不要擅自把 `Hermes` 升级成桌面控制器、消息网关或常驻平台

## 7. 下一步建议

如果要继续推进，优先顺序建议是：

1. 当前 `Phase 0` 已不需要再重复同类 runtime 探针；`P0-2` replacement runtime model 已 accepted，`P0-1 cap_drop:[ALL]` 也已在新 accepted 线复核为仍 blocked，`P0-3` 已收口为 `platform_na / 非 Phase 1 前提`
   - 如果继续推进，下一刀应转入 `Phase 1` 的纯本地 MVP 开发或仅做文档/证据维护，而不是再把 `P0-1` 当作待判定问题重复重跑
2. 先做 `Doc Sync`；`Wave 0` 与 `Wave 1 smoke` 可并行，但 `Phase 1 accepted` 须在 `Phase 0` 全绿后生效
3. `Phase 1` 当前只允许纯本地任务自动执行；`network_proxy` 未证实前不把联网任务写进 MVP
4. `Wave 1` 当前已完成 repo-side smoke / acceptance 补齐：
  - `W1-T06` 样本任务集已补齐
  - `W1-T07` 验收脚本已补齐
  - 下一步只剩判断是否在 `Phase 0` 全绿后进入 live `Codex SDK` 执行验收
5. 如果要彻底消除默认 `--oneshot` 问题，单独向 Hermes upstream 追 provider 自动识别 bug，而不是删除当前 bridge 侧缓解
6. 原生 automations 目前只能当“后续减自建量候选”，不能替代首版 watcher/glue
7. 如果以后真要在 `Ubuntu-24.04` 里直接跑 `docker`，再补 Docker Desktop 的 per-distro WSL integration
8. 如确有消息入口需求，再单独评估是否把 OpenClaw 从“参考仓储备”升级为主链渠道层候选
9. 如再次刷新参考仓，优先复用 `scripts/refresh-reference-repos.ps1`，默认只刷 core；只有本次任务真的涉及二线/条件仓时再显式扩展

## 8. 最短恢复路径

下一位接手时，先做这四步：

1. 打开 [接手检查单.md](D:/CODE/hermes-agent/docs/接手检查单.md)
2. 打开 [工作交接提示词.md](D:/CODE/hermes-agent/docs/工作交接提示词.md)
3. 先读 [Hermes-v2-终态收口-20260628.md](D:/CODE/hermes-agent/docs/Hermes-v2-终态收口-20260628.md)
4. 再决定当前要做的是：
   - 文档维护
   - 只读核查
   - 单变量升级
   - 真正重跑 bring-up

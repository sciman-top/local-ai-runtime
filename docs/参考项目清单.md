# 参考项目清单

这份清单记录当前这条 `Codex + Hermes + AgentBridge` 主链值得保留在本机的参考源码，以及它们各自处于哪一层。

## 放置位置

固定放在：

- `D:\CODE\external\hermes-agent-references`

不要放在：

- `D:\CODE\local-ai-dev-orchestrator`

原因是维护仓与上游参考仓的职责不同：

- `D:\CODE\local-ai-dev-orchestrator` 保存方案、计划、清单、快照和维护索引
- `D:\CODE\external\hermes-agent-references` 保存可更新的上游源码真值

## 当前已拉取

这些参考仓统一放在：

- `D:\CODE\external\hermes-agent-references`

但它们不应被一视同仁。当前更合理的维护方式，是分成 `core`、`secondary`、`conditional` 三层。

### Core：当前主链默认参考面

这些仓属于当前主链的默认参考面。以后做日常核对、只读刷新、升级前比对时，默认先看、默认先刷新这组。

1. `hermes-agent`
   - 位置：`D:\CODE\external\hermes-agent-references\hermes-agent`
   - 价值：Hermes 官方运行时真值
2. `codex`
   - 位置：`D:\CODE\external\hermes-agent-references\codex`
   - 价值：主执行层行为真值
3. `modelcontextprotocol`
   - 位置：`D:\CODE\external\hermes-agent-references\modelcontextprotocol`
   - 价值：MCP 协议层真值
4. `servers`
   - 位置：`D:\CODE\external\hermes-agent-references\servers`
   - 价值：MCP server 官方实现参考

### Secondary：保留，但不属于默认刷新集合

这些仓当前仍值得保留，但已经越过“v1 主链默认参考面”。

5. `skills`
   - 位置：`D:\CODE\external\hermes-agent-references\skills`
   - 价值：skills 设计与落地参考
   - 当前定位：只有在沉淀 `skills-drafts/`、复核 skill 结构或准备吸收 Hermes 产出时才优先查看
6. `hermes-agent-self-evolution`
   - 位置：`D:\CODE\external\hermes-agent-references\hermes-agent-self-evolution`
   - 价值：后续学习层、自进化和 skill 改写方向
   - 当前定位：属于未来学习闭环增强储备，不是 bring-up、boundary、snapshot 的默认排查仓

### Conditional：条件保留

这些仓可以继续保留，但不应再被当作当前主链活跃参考。

7. `openclaw`
   - 位置：`D:\CODE\external\hermes-agent-references\openclaw`
   - 价值：渠道层储备
   - 当前结论：仅在 Telegram / Slack / WhatsApp 等真实消息入口需求出现后，再升级为活跃候选

## 当前不应并入本参考架的仓

以下项目即便以后要参考，也不建议并入 `D:\CODE\external\hermes-agent-references`：

- `AstrBot`
- `NapCat-Docker`
- `NapCatQQ`
- `aiocqhttp`

原因：

- 它们服务的是 QQ / OneBot / IM 渠道层，而不是当前 `Hermes + Codex + AgentBridge` 主链
- 当前仓文档已经把 `OpenClaw` 和消息入口明确标成延后能力
- 本机已存在专属参考架：`D:\CODE\external\qq-codex-bot-references`

如果以后只是需要查 QQ 渠道层实现，应直接复用那套专属参考架，而不是在这里重复拉一份

## 维护顺序建议

以后出现问题时，不要同时翻所有仓。优先顺序是：

1. `hermes-agent`
2. `codex`
3. `modelcontextprotocol`
4. `servers`
5. `skills`
6. `hermes-agent-self-evolution`
7. `openclaw`

每个参考仓“本机为什么保留它”的短观察记录见：

- [references/observations/README.md](D:/CODE/local-ai-dev-orchestrator/references/observations/README.md)

## 本次拉取锚点

当前本机 head：

- `hermes-agent`: `f53b184c48`
- `codex`: `328e95110c`
- `modelcontextprotocol`: `ead35b59`
- `servers`: `7b1170d`
- `skills`: `49f948f`
- `hermes-agent-self-evolution`: `0a929e3`
- `openclaw`: `1bdde66950`

更详细的现场索引见：

- [D:\CODE\external\hermes-agent-references\README.md](D:/CODE/external/hermes-agent-references/README.md)
- [references/updates/reference-refresh-latest.md](D:/CODE/local-ai-dev-orchestrator/references/updates/reference-refresh-latest.md)

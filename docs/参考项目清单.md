# 参考项目清单

这份清单记录当前 **local AI Dev Orchestrator 主线** 值得保留在本机的参考源码，以及它们各自处于哪一层。

## 放置位置

固定放在：

- `D:\CODE\external\local-ai-dev-orchestrator-references`

不要放在：

- `D:\CODE\local-ai-dev-orchestrator`

原因是维护仓与上游参考仓的职责不同：

- `D:\CODE\local-ai-dev-orchestrator` 保存方案、计划、清单、快照和维护索引
- `D:\CODE\external\local-ai-dev-orchestrator-references` 保存可更新的上游源码真值

## 当前已拉取

这些参考仓统一放在：

- `D:\CODE\external\local-ai-dev-orchestrator-references`

治理 sidecar companion 单独位于：

- `D:\CODE\governed-ai-coding-runtime`

但它们不应被一视同仁。当前更合理的维护方式，是分成 `core-mainline`、`secondary-mainline`、`governance-sidecar`、`compatibility`、`archive-on-demand` 五层。

### Core Mainline：当前主链默认参考面

这些仓属于当前主链的默认参考面。以后做日常核对、只读刷新、升级前比对时，默认先看、默认先刷新这组。

1. `codex`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\codex`
   - 价值：主执行层行为真值
2. `plugins`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\plugins`
   - 价值：当前 Codex plugin / hook / skill-only plugin 打包示例真值
3. `modelcontextprotocol`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\modelcontextprotocol`
   - 价值：MCP 协议层真值
4. `servers`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\servers`
   - 价值：MCP server 官方实现参考

### Secondary Mainline：保留，但不属于默认刷新集合

这些仓当前仍值得保留，但还没进入“默认每次都先刷新”的集合。

5. `openai-agents-python`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\openai-agents-python`
   - 价值：多 agent、handoff、guardrail、session、sandbox 设计参考
   - 当前定位：只有在推进 `gpt54_direct` planner / review / handoff 等更高层 orchestration 能力时才优先查看

### Governance Sidecar：正式 companion，但不进入默认刷新集合

6. `governed-ai-coding-runtime`
   - 位置：`D:\CODE\governed-ai-coding-runtime`
   - 价值：gate / evidence / selector / preflight 治理机制参考
   - 当前定位：当前仓正式 `governance-sidecar` companion；只有在推进 Governance Overlay 时才优先查看

### Compatibility：兼容线真值

7. `hermes-agent`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\hermes-agent`
   - 价值：Hermes/AgentBridge 兼容线运行时真值
   - 当前定位：当前 generic orchestrator mainline 不再默认先看它；只有兼容线、boundary、digest 升级或 Hermes 运行时问题时才优先查看

### Archive / On-Demand：保留，但不再是活跃默认面

这些仓可以继续保留，但不应再被当作当前主线活跃参考。

8. `openclaw`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\openclaw`
   - 价值：渠道层储备
   - 当前结论：仅在 Telegram / Slack / WhatsApp 等真实消息入口需求出现后，再升级为活跃候选
9. `skills`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\skills`
   - 价值：旧 skill 结构回看
   - 当前结论：上游已 deprecated；当前官方主入口已切到 `plugins`
10. `hermes-agent-self-evolution`
   - 位置：`D:\CODE\external\local-ai-dev-orchestrator-references\hermes-agent-self-evolution`
   - 价值：未来学习层、自进化和 skill 改写方向
   - 当前结论：属于未来增强储备，不是当前主线默认排查仓

### Conditional Not Cloned：有需要再拉

11. `claude-code`
   - 当前状态：暂未拉取
   - 当前结论：只有当 `claude_glm` review adapter 进入真实编码队列时，再补这类官方参考面

## 当前不应并入本参考架的仓

以下项目即便以后要参考，也不建议并入 `D:\CODE\external\local-ai-dev-orchestrator-references`：

- `AstrBot`
- `NapCat-Docker`
- `NapCatQQ`
- `aiocqhttp`

原因：

- 它们服务的是 QQ / OneBot / IM 渠道层，而不是当前 `Hermes + Codex + AgentBridge` 主链
- 当前仓文档已经把 `OpenClaw` 和消息入口明确标成延后能力
- 本机已存在专属参考架：`D:\CODE\external\qq-codex-bot-references`

如果以后只是需要查 QQ 渠道层实现，应直接复用那套专属参考架，而不是在这里重复拉一份

## 维护顺序建议

以后出现问题时，不要同时翻所有仓。优先顺序是：

1. `codex`
2. `plugins`
3. `modelcontextprotocol`
4. `servers`
5. `openai-agents-python`
6. `governed-ai-coding-runtime`
7. `hermes-agent`
8. `skills`
9. `hermes-agent-self-evolution`
10. `openclaw`

每个参考仓“本机为什么保留它”的短观察记录见：

- [references/observations/README.md](D:/CODE/local-ai-dev-orchestrator/references/observations/README.md)

## 本次拉取锚点

当前本机 head：

- `codex`: `be33f80bc6`
- `plugins`: `d6169bef12`
- `modelcontextprotocol`: `60dc69e9a9`
- `servers`: `7097923966`
- `openai-agents-python`: `1643dbe617`
- `governed-ai-coding-runtime`: `390771774b` (`main`)
- `hermes-agent`: `f53b184c48`
- `skills`: `49f948f`
- `hermes-agent-self-evolution`: `0a929e3`
- `openclaw`: `1bdde66950`

更详细的现场索引见：

- [D:\CODE\external\local-ai-dev-orchestrator-references\README.md](D:/CODE/external/local-ai-dev-orchestrator-references/README.md)
- [references/updates/reference-refresh-latest.md](D:/CODE/local-ai-dev-orchestrator/references/updates/reference-refresh-latest.md)

# Artifacts

Put only task-specific output artifacts here.

Do not store secrets in this directory.

Each result file that references an artifact must also record:

- relative path
- byte size
- sha256
